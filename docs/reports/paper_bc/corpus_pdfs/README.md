# Retrieved corpus PDFs

One file per coded paper, named `<citation_key>.pdf` (see ../CORPUS_PDF_RETRIEVAL.md).
The citation key is the join between the PDF, the bibliography entry, and the
`record_slug` column of `paper_bc_review_corpus.csv`.
