# Screenshot Instructions

Since I cannot interact with the GUI to take screenshots, please manually capture the following and save them to the `pr_evidence/frontend/` or `pr_evidence/backend/` directories as indicated.

## Frontend Screenshots (`pr_evidence/frontend/`)

1.  **`home_page.png`**: The main dashboard landing page showing the list of experiments.
2.  **`experiment_detail.png`**: The detailed view of a specific experiment (e.g., Adult Income / Random Forest).
3.  **`lime_viz.png`**: A focused screenshot of the LIME explanation bar chart.
4.  **`shap_viz.png`**: A focused screenshot of the SHAP waterfall or force plot.
5.  **`comparison.png`**: The view showing LIME and SHAP side-by-side (if available).
6.  **`user_flow.gif`** (Optional): A short screen recording of navigating from the list to a detail view.

## Backend Screenshots (`pr_evidence/backend/`)

1.  **`swagger_ui.png`**: The automatic API documentation at `http://localhost:8000/docs`.
2.  **`api_response.png`**: A screenshot of a raw JSON response (e.g., from Postman or browser) for `GET /api/runs`.
3.  **`coverage_report.png`**: The HTML coverage report summary (if generated).

## Verification
Once saved, these images will be referenced by the `PR_DESCRIPTION.md` files I have generated.
